# CI: lint (incl. jsx-a11y), typecheck, build (SPEC §15 ci.yml). All steps run
# in the app subdirectory salone-explorer/ (SPEC §12).
name: ci

on:
  push:
    branches: [dev, main]
  pull_request:
    branches: [dev, main]

defaults:
  run:
    working-directory: salone-explorer

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: salone-explorer/package-lock.json
      - run: npm ci
      - run: npm run lint
      - run: npm run typecheck
      - run: npm run test
      # Fail if the Edge Function email-copy snapshot drifts from the content
      # layer (ADR 0007 D2): regenerate and assert no diff.
      - run: npm run check:email-copy
      - run: npm run build
